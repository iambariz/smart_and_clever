import Toybox.Graphics;
import Toybox.Lang;

// Common contract for a hand's geometry (
class HandShape {
    function initialize() {
    }

    function draw(dc as Dc, centerX as Number, centerY as Number, angle as Float, length as Float, width as Number) as Void {
    }
}
